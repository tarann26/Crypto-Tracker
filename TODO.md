- paper trades only live in backend memory, keyed by a session id in
  localstorage. restart the backend or clear site data and the ledger
  is gone. needs a real db at some point, sqlite is probably fine
